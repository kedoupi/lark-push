# Copy to ~/.config/lark-push/config.env or export in your shell.

# Required unless you always pass --chat-id
LARK_PUSH_CHAT_ID=oc_xxxxxxxx

# bot (default) or user
LARK_PUSH_AS=bot

# Card footer caption
LARK_PUSH_FOOTER=via lark-push
